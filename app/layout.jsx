export const metadata = { title: "IG Pro — JES Group", description: "专业内容情报系统" };
export default function RootLayout({ children }) {
  return (<html lang="zh"><body style={{margin:0,padding:0}}>{children}</body></html>);
}
